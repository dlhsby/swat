# Defaults consumed by docker-compose.yml (`--env-file infra/docker-compose.env`).
# Keep these consistent with the root .env.local used by the apps.
POSTGRES_USER=swat
POSTGRES_PASSWORD=swat_dev_password
POSTGRES_DB=swat

MINIO_ROOT_USER=swat
MINIO_ROOT_PASSWORD=swat_dev_secret
