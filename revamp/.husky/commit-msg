#!/usr/bin/env sh
# Resolve the commit-message file to an absolute path before switching dirs,
# then validate it from the monorepo root (see note in pre-commit).
MSG_FILE="$(cd "$(dirname "$1")" && pwd)/$(basename "$1")"
cd "$(dirname "$0")/.." || exit 1
pnpm exec commitlint --edit "$MSG_FILE"
