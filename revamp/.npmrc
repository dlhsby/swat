# pnpm configuration for the SWAT monorepo.
# Hoist Prisma to the root node_modules so the generated client and CLI
# resolve consistently across workspaces.
public-hoist-pattern[]=*prisma*
public-hoist-pattern[]=*@prisma*
auto-install-peers=true
strict-peer-dependencies=false
dedupe-peer-dependents=true
